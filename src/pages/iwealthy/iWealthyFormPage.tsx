// src/pages/iwealthy/IWealthyFormPage.tsx


import FormInputSection from "../../components/FormInputSection";

// --- REMOVED ---
// ไม่ต้องใช้ useEffect, useNavigate, หรือ useAppStore ที่นี่อีกต่อไป

export default function IWealthyFormPage() {
  
  // --- REMOVED ---
  // useEffect ที่เคยทำให้เกิดการ redirect ได้ถูกลบออกไปแล้ว
  // ทำให้ผู้ใช้สามารถกลับมาที่หน้านี้เพื่อแก้ไขข้อมูลได้ตามปกติ

  return (
      <FormInputSection /> 
  );
}